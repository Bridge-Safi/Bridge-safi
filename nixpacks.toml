[variables]
NODE_VERSION = "20"

[phases.install]
cmds = ["npm install -g pnpm@10", "node /root/.npm-global/bin/pnpm install --no-frozen-lockfile"]

[phases.build]
cmds = ["node /root/.npm-global/bin/pnpm run build:railway"]
